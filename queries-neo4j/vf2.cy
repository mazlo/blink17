MATCH (studyLabel)<-[:PREFLABEL]-(s:Study)-[:INGROUP]->(:StudyGroup)-[:PREFLABEL]->(studyGroupLabel), (s)-[:PRODUCT]->(lds:LogicalDataSet)-[:PREFLABEL]->(datasetLabel), (lds)-[:VARIABLE]->(variable)-[:REPRESENTATION]->(:ConceptScheme)-[:HASTOPCONCEPT]->(:Concept)<-[:STATISTICSCATEGORY]-(:CategoryStatistics)-[:SPATIAL]->(country) WHERE studyGroupLabel.en = 'CIS' AND studyLabel.en = '2008' AND datasetLabel.en = 'cross-sec' RETURN DISTINCT country.label ORDER BY country.label;
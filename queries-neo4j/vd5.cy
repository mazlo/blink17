MATCH (studyLabel)<-[:PREFLABEL]-(s:Study)-[:INGROUP]->(sg:StudyGroup)-[:PREFLABEL]->(studyGroupLabel), (s)-[:PRODUCT]->(lds:LogicalDataSet)-[:PREFLABEL]->(datasetLabel), (lds)-[:VARIABLE]->(variable)-[:QUESTION]->(question)-[:QUESTIONTEXT]->(qt) WHERE studyGroupLabel.en = 'CIS' AND studyLabel.en = '2008' AND datasetLabel.en = 'cross-sec' AND variable.notation = 'RDENG' RETURN DISTINCT studyLabel.en, qt.en;